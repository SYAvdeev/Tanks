using Domain.Logic;
using Domain.Models;

namespace Features.Level
{
    public class LevelViewModel : BaseViewModel
    {
        public LevelViewModel(IModel model, ILogicCollection logicCollection) : base(model, logicCollection)
        {
        }
    }
}